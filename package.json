{
  "name": "stealthweb",
  "version": "1.0.0",
  "description": "StealthWeb – A browser that resets its identity every 30 seconds.",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . StealthWeb --platform=win32 --arch=x64"
  },
  "dependencies": {
    "electron": "^31.0.0"
  }
}